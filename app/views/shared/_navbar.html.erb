<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="\"><b><i style="color: #0d6efd; font-size: 35px">EZ</i><i style="color: #0da955; font-size: 35px">mem!</i></b></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "My cards", cards_path, :class => "nav-link" %>
        </li>
      </ul>
    </div>
    <% if current_user.present? %>     
      <%= button_to("Log Out", destroy_user_session_path, :method => :delete, :class => "btn btn-outline-success") %>
    <% else %>
      <%= button_to("Log In", user_session_path, :class => "btn btn-outline-success") %>
    <%end%>
  </div>
</nav>
