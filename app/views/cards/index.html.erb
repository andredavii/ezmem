<p style="color: green"><%= notice %></p>

<h1 style="text-align: center; margin-right: 460px;">Cards</h1>
<div style="text-align: center; margin-right: -465px;">
<%= link_to "New card", new_card_path, class: "btn btn-primary" %>
</div>
<div id="cards" >
  <% @cards.each do |card| %>
    <div class="card">
    <%= render card %>
    <p>
    <div class="btn-group">
    <%= link_to "Show this card", card, class: "btn btn-primary" %> 
    <%= link_to "Learn this card", learn_card_path(card), class: "btn btn-success" %>
    </div>
    </p>
    </div>
  <% end %>
</div>

