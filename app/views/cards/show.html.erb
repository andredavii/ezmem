<p style="color: green"><%= notice %></p>

<div class="card">
<%= render @card %>


<div>
  <%= link_to "Edit", edit_card_path(@card) %> |
  <%= link_to "Learn", learn_card_path(@card) %> |
  <%= link_to "Back", cards_path %> 
  <div style ="text-align: right;">
  <%= button_to "Delete", @card, method: :delete, class: "btn btn-danger" %>
  </div>
</div>
